# Build outputs (TypeScript compilation)
dist/*.js
dist/*.d.ts
dist/*.map
dist/tsconfig.lib.tsbuildinfo

# Keep WASM files checked into git
!dist/generated/

# Temporary build directories
vault-wasm-temp/
wasm-build-output/
